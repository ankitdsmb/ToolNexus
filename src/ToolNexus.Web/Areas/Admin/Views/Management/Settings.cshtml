@{
    ViewData["Title"] = "Settings";
}

<div class="card">
    <div class="card-body">
        <div class="d-flex align-items-center justify-content-between gap-3 mb-3">
            <div>
                <h3 class="card-title mb-1">Platform Settings</h3>
                <p class="text-secondary mb-0">Global admin settings are currently read-only and managed outside this shell.</p>
            </div>
            <span class="badge bg-yellow-lt">Coming soon</span>
        </div>

        <p class="mb-2"><strong>Status:</strong> Settings write APIs are intentionally withheld to prevent unsafe drift.</p>
        <p class="mb-2"><strong>Data:</strong> Configuration snapshots are not exposed in this release.</p>
        <p class="text-secondary mb-0">Future capability: environment-scoped configuration controls with approval workflow.</p>
    </div>
</div>
