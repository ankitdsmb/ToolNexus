@model string
@{
    ViewData["Title"] = Model;
}

<div class="card">
    <div class="card-body">
        <h3 class="card-title">@Model</h3>
        <p class="text-secondary mb-0">Placeholder section for upcoming admin workflows. No CRUD or API wiring is included in this phase.</p>
    </div>
</div>
