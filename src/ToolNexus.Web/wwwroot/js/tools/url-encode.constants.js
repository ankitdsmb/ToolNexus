export const URL_ENCODE_MODES = Object.freeze({
  component: 'component',
  fullUrl: 'full-url',
  queryValues: 'query-values'
});

export const SPACE_ENCODING = Object.freeze({
  percent20: 'percent20',
  plus: 'plus'
});

export const PROCESSING_LIMITS = Object.freeze({
  warningChars: 50_000
});

export const KEYBOARD = Object.freeze({
  ENTER: 'Enter',
  KEY_L: 'l'
});
