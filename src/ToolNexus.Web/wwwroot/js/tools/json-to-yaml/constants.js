export const INDENT_CHOICES = Object.freeze([2, 4]);

export const MULTILINE_STYLE = Object.freeze({
  literal: 'literal',
  folded: 'folded'
});

export const MAX_SAFE_DEPTH = 200;
export const LARGE_INPUT_THRESHOLD = 200_000;
