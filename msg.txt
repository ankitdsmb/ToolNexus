Fix CI Workflow Property Quote Issue

- Quoted the entire `/p:Include` MSBuild property argument in `.github/workflows/ci.yml` to prevent shell expansion issues.
- Verified that all tests continue to pass locally.
