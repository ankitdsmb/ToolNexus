# Tool Scaffolding Intelligence

Use the local CLI wrapper to scaffold tools:

```bash
./scripts/toolnexus create-tool <toolId> [--template utility|structured|custom-ui]
```

The generator emits manifest metadata, runtime module/template assets, default icon, server execution stub, docs, and initial tests with governance-safe defaults.
